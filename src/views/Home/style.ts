import styled from 'styled-components'

export const HomeWrapper = styled.div`
  & {
    padding: 0 50px;
  }

  .showcase {
    display: flex;
    justify-content: center;
    flex-wrap: wrap;
  }
`
